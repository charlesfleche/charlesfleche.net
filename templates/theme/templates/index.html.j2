{% extends "base.html.j2" %}

{% block main %}
<article>
  <header>
    <h1>{{ title }}</h1>
  </header>

  {{ content_path | read_text }}

  <h1>Articles</h1>

  {% for article in articles.by_category.article %}
  <p>
    <time datetime="{{ article.date }}">{{ article.date }}</time>
    <a href={{ article.path }}>{{ article.title }}</a><br />{{ article.description }}
  </p>
  {% endfor %}
</article>


{% endblock main %}